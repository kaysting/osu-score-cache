<!DOCTYPE html>
<html lang="en">

<!-- Gemini created the styling for this site, full disclosure -->

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= locals.title || '' %></title>
    <meta property="og:site_name" content="<%= locals.meta?.siteName || '' %>">
    <meta property="og:title" content="<%= locals.meta?.title || '' %>">
    <meta property="og:description" content="<%= locals.meta?.description || '' %>">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Roboto+Mono:wght@400;500&display=swap"
        rel="stylesheet">
</head>

<style>
    /* CSS Variables */
    :root {
        /* Light Mode */
        --bg-void: #ffffff;
        /* Changed to match canvas (was #f3f6f9) */
        --bg-canvas: #ffffff;
        --fg-default: #1f2328;
        --fg-muted: #656d76;
        --border-default: #d0d7de;
        --border-muted: #d8dee4;

        --bg-code-inline: #afb8c133;
        --bg-code-block: #f6f8fa;

        --color-link: #0969da;
        --color-header-border: #d0d7de;

        --table-row-even: #f6f8fa;
    }

    /* Dark Mode */
    @media (prefers-color-scheme: dark) {
        :root {
            --bg-void: #0d1117;
            /* Changed to match canvas (was #010409) */
            --bg-canvas: #0d1117;
            --fg-default: #e6edf3;
            --fg-muted: #848d97;
            --border-default: #30363d;
            --border-muted: #21262d;

            --bg-code-inline: #6e768166;
            --bg-code-block: #161b22;

            --color-link: #4493f8;
            --color-header-border: #21262d;

            --table-row-even: #161b22;
        }
    }

    body {
        background-color: var(--bg-void);
        color: var(--fg-default);
        margin: 0;
        padding: 0;
        font-family: 'Inter', sans-serif;
        transition: background-color 0.3s ease, color 0.3s ease;
    }

    .main-container {
        max-width: 900px;
        margin: 0 auto;
        padding: 40px 24px;
    }

    .markdown-body {
        /* Removed card styling (border, background, radius) */
        background-color: transparent;
        border: none;
        padding: 0;
        /* Removing internal padding, relying on main-container */
        font-size: 16px;
        line-height: 1.6;
        word-wrap: break-word;
    }

    @media (max-width: 767px) {
        .main-container {
            padding: 20px;
            /* Simplified mobile padding */
        }
    }

    /* Typography */
    .markdown-body h1,
    .markdown-body h2,
    .markdown-body h3,
    .markdown-body h4,
    .markdown-body h5,
    .markdown-body h6 {
        margin-top: 24px;
        margin-bottom: 16px;
        font-weight: 600;
        line-height: 1.25;
        color: var(--fg-default);
        letter-spacing: -0.01em;
    }

    .markdown-body h1 {
        font-size: 2em;
        padding-bottom: 0.3em;
        border-bottom: 1px solid var(--color-header-border);
    }

    .markdown-body h2 {
        font-size: 1.5em;
        padding-bottom: 0.3em;
        border-bottom: 1px solid var(--color-header-border);
    }

    .markdown-body p {
        margin-top: 0;
        margin-bottom: 16px;
    }

    /* Links */
    .markdown-body a {
        color: var(--color-link);
        text-decoration: none;
        font-weight: 500;
    }

    .markdown-body a:hover {
        text-decoration: underline;
    }

    /* Lists */
    .markdown-body ul,
    .markdown-body ol {
        margin-top: 0;
        margin-bottom: 16px;
        padding-left: 2em;
    }

    .markdown-body li {
        margin-top: 0.25em;
    }

    /* Blockquotes */
    .markdown-body blockquote {
        margin: 0 0 16px;
        padding: 0 1em;
        color: var(--fg-muted);
        border-left: 0.25em solid var(--border-default);
    }

    /* Code and Pre */
    .markdown-body code {
        padding: 0.1em 0.3em;
        margin: 0;
        font-size: 92%;
        background-color: var(--bg-code-inline);
        border-radius: 6px;
        font-family: 'Roboto Mono', monospace;
    }

    .markdown-body pre {
        padding: 16px;
        overflow: auto;
        font-size: 92%;
        line-height: 1.45;
        background-color: var(--bg-code-block);
        border-radius: 6px;
        margin-bottom: 16px;
        /* Keep border on code blocks for contrast against the plain background */
        border: 1px solid var(--border-default);
    }

    .markdown-body pre code {
        background-color: transparent;
        padding: 0;
        border-radius: 0;
        word-break: normal;
        white-space: pre;
        color: var(--fg-default);
    }

    /* Horizontal Rules */
    .markdown-body hr {
        height: 0.25em;
        padding: 0;
        margin: 24px 0;
        background-color: var(--border-default);
        border: 0;
    }

    /* Tables */
    .markdown-body table {
        border-spacing: 0;
        border-collapse: collapse;
        display: block;
        width: max-content;
        max-width: 100%;
        overflow: auto;
        margin-bottom: 16px;
    }

    .markdown-body table th,
    .markdown-body table td {
        padding: 6px 13px;
        border: 1px solid var(--border-default);
    }

    .markdown-body table tr {
        background-color: var(--bg-canvas);
        border-top: 1px solid var(--border-muted);
    }

    .markdown-body table tr:nth-child(2n) {
        background-color: var(--table-row-even);
    }

    /* Images */
    .markdown-body img {
        max-width: 100%;
        box-sizing: content-box;
        background-color: var(--bg-canvas);
        border-style: none;
    }
</style>

<body>
    <div class="main-container">
        <article class="markdown-body">
            <%- content %>
        </article>
    </div>
</body>

</html>